HPC_USER="umcg-jvnijnatten"

alias calculon='ssh -A lobby+calculon'
alias mntcalculon='mkdir -p ~/Mounts/calculon; sshfs lobby+calculon:/groups/umcg-griac/tmp04/projects/$HPC_USER ~/Mounts/calculon'
alias umntcalculon='diskutil unmount force ~/Mounts/calculon'

alias gearshift='ssh -A airlock+gearshift'
alias mntgearshift='mkdir -p ~/Mounts/gearshift; sshfs airlock+gearshift:/groups/umcg-griac/tmp01/projects/$HPC_USER ~/Mounts/gearshift'
alias umntgearshift='diskutil unmount force ~/Mounts/gearshift'

alias ftp_='sftp -P 22 $HPC_USER@cher-ami.hpc.rug.nl'

# scp ~/.vimrc lobby+calculon:/home/$HPC_USER/.vimrc
